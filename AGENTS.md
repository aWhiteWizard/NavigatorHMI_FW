# NavigatorHMI_FW 工作区指令

## PDCA 纪律（用户强化，2026-08-19）

1. **Plan 必须包含上轮 Check 未通过项**：每次 Plan 时，把上一轮 Check 未通过/未实现的事项
   （PROJECT.md「Check 发现未实现集中清单」）**全部纳入本轮任务**，继续去做，不允许遗漏搁置。
   完成一项从清单划掉，新增 Check 发现项持续追加。
2. **环节切换只由用户明确发起**：Plan → Do → Check → Act 各环节，只有用户明确说
   「进入下一阶段/开始执行」才推进；用户说方向/要求 = 只记录不实施（尤其 Check 环节严禁改代码）。
3. **Do 第一步建 todo 清单**：进入 Do 先建任务分解表，随进度更新。
4. **小步提交、每步审查**：每批改动编译 + 测试 + reviewer 审查 + 提交（message 无 BOM）。
5. **Act 落库 + 提交**：经验教训写知识库（4_bugs/5_sessions/2_system），PROJECT.md 更新继续点。
6. **push 时机等用户决定**（Act 结束或用户明确要求）。
